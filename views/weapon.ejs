<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= weapon.name[lang] %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main>
        <section class="detail-header">
            <div class="container">
                <h1><i class="fas fa-gun"></i> <%= weapon.name[lang] %></h1>
            </div>
        </section>

        <div class="container py-5">
            <div class="row">
                <div class="col-lg-8 mx-auto">
                    <div class="detail-content">
                        <% if(weapon.picture && weapon.picture.includes("https")) { %>
                            <img src="<%= weapon.picture %>" class="card-img-top" alt="<%= weapon.name[lang] %>" loading="lazy">
                        <% } else { %>
                            <img src="/pictures/<%= weapon.picture %>" class="card-img-top" alt="<%= weapon.name[lang] %>" loading="lazy">
                        <% } %>
                        <div class="detail-meta">
                            <strong><i class="fas fa-list"></i> <%= lang === 'hu' ? 'Típus:' : lang === 'ro' ? 'Tip:' : 'Type:' %></strong> <%= weapon.type[lang] %>
                        </div>

                        <p><%= weapon.description[lang] %></p>

                        <a href="/weapons?lang=<%= lang %>" class="btn btn-danger">
                            <i class="fas fa-arrow-left"></i> <%= lang === 'hu' ? 'Vissza a fegyverekhez' : lang === 'ro' ? 'Înapoi la arme' : 'Back to Weapons' %>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2026 WWII History</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>