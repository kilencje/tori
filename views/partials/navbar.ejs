<nav class="navbar navbar-dark navbar-expand-lg sticky-top">
    <div class="container-fluid">
        <!-- Brand / Home -->
        <a class="navbar-brand fw-bold" href="/?lang=<%= lang %>">
            <i class="fas fa-bomb"></i> 
            <%= lang === 'hu' ? 'WWII Történelem' : lang === 'ro' ? 'WWII Istorie' : 'WWII History' %>
        </a>
        
        <!-- Toggler for mobile -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navigation items -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto gap-2">

                <!-- Home Link -->
                <li class="nav-item">
                    <a class="nav-link" href="/?lang=<%= lang %>">
                        <i class="fas fa-home"></i> 
                        <%= lang === 'hu' ? 'Főoldal' : lang === 'ro' ? 'Acasă' : 'Home' %>
                    </a>

                <!-- Content Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="contentDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-book"></i> 
                        <%= lang === 'hu' ? 'Tartalom' : lang === 'ro' ? 'Conținut' : 'Content' %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="contentDropdown">
                        <li>
                            <a class="dropdown-item" href="/battles?lang=<%= lang %>">
                                <i class="fas fa-person-military-rifle"></i> 
                                <%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %>
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/weapons?lang=<%= lang %>">
                                <i class="fas fa-gun"></i> 
                                <%= lang === 'hu' ? 'Fegyverek' : lang === 'ro' ? 'Arme' : 'Weapons' %>
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/technology?lang=<%= lang %>">
                                <i class="fas fa-radiation"></i> 
                                <%= lang === 'hu' ? 'Technológia' : lang === 'ro' ? 'Tehnologie' : 'Technology' %>
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/quiz?lang=<%= lang %>">
                                <i class="fas fa-brain"></i> 
                                <%= lang === 'hu' ? 'Kvíz' : lang === 'ro' ? 'Quiz' : 'Quiz' %>
                            </a>
                        </li>
                    </ul>
                </li>

                <!-- Language Dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="langDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-globe"></i> 
                        <%= lang === 'hu' ? 'Magyar' : lang === 'ro' ? 'Română' : 'English' %>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark dropdown-menu-end" aria-labelledby="langDropdown">
                        <li>
                            <a class="dropdown-item" href="<%= currentPath %>?lang=en">
                                <i class="fas fa-flag"></i> English
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="<%= currentPath %>?lang=hu">
                                <i class="fas fa-flag"></i> Magyar
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="<%= currentPath %>?lang=ro">
                                <i class="fas fa-flag"></i> Română
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
