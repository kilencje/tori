<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main>
        <div class="container py-5">
            <div class="row mb-4">
                <div class="col-12">
                    <h1 class="page-title">
                        <i class="fas fa-crossed-swords"></i> <%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %>
                    </h1>
                </div>
            </div>

            <div class="row g-4">
                <% battles.forEach(battle => { %>
                    <div class="col-lg-6 col-md-12">
                        <div class="card item-card shadow-lg">
                            <% if(battle.picture && battle.picture.includes("https")) { %>
                                <img src="<%= battle.picture %>" class="card-img-top" alt="<%= battle.name[lang] %>">
                            <% } else { %>
                                <img src="/pictures/<%= battle.picture %>" class="card-img-top" alt="<%= battle.name[lang] %>">
                            <% } %>
                            <div class="card-body">
                                <h5 class="card-title"><i class="fas fa-flag-usa"></i> <%= battle.name[lang] %></h5>
                                <p class="card-text"><%= battle.description[lang] %></p>
                                <div class="detail-meta">
                                    <strong><i class="fas fa-calendar"></i> <%= lang === 'hu' ? 'Év:' : lang === 'ro' ? 'An:' : 'Year:' %></strong> <%= battle.year %>
                                </div>
                                <a href="/battle/<%= battle.id %>?lang=<%= lang %>" class="btn btn-primary btn-sm">
                                    <%= lang === 'hu' ? 'Megtekintés' : lang === 'ro' ? 'Vezi' : 'View' %> <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </main>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2026 WWII History</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>