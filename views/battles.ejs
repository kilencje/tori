<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %></title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div class="navbar">
            <h1><%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %></h1>
            <ul>
                <li><a href="/?lang=<%= lang %>"><%= lang === 'hu' ? 'Főoldal' : lang === 'ro' ? 'Acasă' : 'Home' %></a></li>
                <li><a href="/weapons?lang=<%= lang %>"><%= lang === 'hu' ? 'Fegyverek' : lang === 'ro' ? 'Arme' : 'Weapons' %></a></li>
                <li><a href="/quiz?lang=<%= lang %>"><%= lang === 'hu' ? 'Kvíz' : lang === 'ro' ? 'Quiz' : 'Quiz' %></a></li>
            </ul>
            <div class="lang-switch">
                <a href="?lang=en">EN</a>
                <a href="?lang=hu">HU</a>
                <a href="?lang=ro">RO</a>
            </div>
        </div>
    </header>
    <main>
        <ul>
            <% battles.forEach(battle => { %>
                <li>
                    <h2><a href="/battle/<%= battle.id %>?lang=<%= lang %>"><%= battle.name[lang] %></a></h2>
                    <p><%= battle.description[lang] %></p>
                    <p><%= lang === 'hu' ? 'Év:' : lang === 'ro' ? 'An:' : 'Year:' %> <%= battle.year %></p>
                </li>
            <% }); %>
        </ul>
    </main>
    <footer>
        <p>&copy; 2026 WWII Website</p>
    </footer>
</body>
</html>