<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lang === 'hu' ? 'Fegyverek' : lang === 'ro' ? 'Arme' : 'Weapons' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <nav class="navbar navbar-dark navbar-expand-lg sticky-top">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/?lang=<%= lang %>">
                <i class="fas fa-bomb"></i> <%= lang === 'hu' ? 'WWII Történelem' : lang === 'ro' ? 'WWII Istorie' : 'WWII History' %>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/battles?lang=<%= lang %>">
                            <i class="fas fa-person-military-rifle"></i> <%= lang === 'hu' ? 'Csaták' : lang === 'ro' ? 'Bătălii' : 'Battles' %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/weapons?lang=<%= lang %>">
                            <i class="fas fa-gun"></i> <%= lang === 'hu' ? 'Fegyverek' : lang === 'ro' ? 'Arme' : 'Weapons' %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/quiz?lang=<%= lang %>">
                            <i class="fas fa-brain"></i> <%= lang === 'hu' ? 'Kvíz' : lang === 'ro' ? 'Quiz' : 'Quiz' %>
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="langDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-globe"></i> <%= lang.toUpperCase() %>
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="langDropdown">
                            <li><a class="dropdown-item" href="/weapons?lang=en">English</a></li>
                            <li><a class="dropdown-item" href="/weapons?lang=hu">Magyar</a></li>
                            <li><a class="dropdown-item" href="/weapons?lang=ro">Română</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main>
        <div class="container py-5">
            <div class="row mb-4">
                <div class="col-12">
                    <h1 class="page-title">
                        <i class="fas fa-gun"></i> <%= lang === 'hu' ? 'Fegyverek' : lang === 'ro' ? 'Arme' : 'Weapons' %>
                    </h1>
                </div>
            </div>

            <div class="row g-4">
                <% weapons.forEach(weapon => { %>
                    <div class="col-lg-6 col-md-12">
                        <div class="card item-card shadow-lg">
                            <% if(weapon.picture && weapon.picture.includes("https")) { %>
                            <img src="<%= weapon.picture %>" class="card-img-top" alt="<%= weapon.name[lang] %>">
                            <% } else { %>
                            <img src="/pictures/<%= weapon.picture %>" class="card-img-top" alt="<%= weapon.name[lang] %>">
                            <% } %>
                            <div class="card-body">
                                <h5 class="card-title"><i class="fas fa-hammer"></i> <%= weapon.name[lang] %></h5>
                                <p class="card-text"><%= weapon.description[lang] %></p>
                                <div class="detail-meta">
                                    <strong><i class="fas fa-list"></i> <%= lang === 'hu' ? 'Típus:' : lang === 'ro' ? 'Tip:' : 'Type:' %></strong> <%= weapon.type[lang] %>
                                </div>
                                <a href="/weapon/<%= weapon.id %>?lang=<%= lang %>" class="btn btn-danger btn-sm">
                                    <%= lang === 'hu' ? 'Megtekintés' : lang === 'ro' ? 'Vezi' : 'View' %> <i class="fas fa-arrow-right ms-1"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </div>
    </main>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2026 WWII History</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>